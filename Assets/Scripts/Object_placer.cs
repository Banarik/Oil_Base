using UnityEngine;

public class Object_placer : MonoBehaviour
{
    public GameObject original;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
